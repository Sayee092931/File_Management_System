# File_Management_System
Collection Manager — Python tool to manage up to 100 text files in ./collection_data with a JSON index (created/modified/size/tags). Includes core library, CLI, TUI, Flask web UI and tests. Features: list, show (with line numbers), create, append/remove lines, search, tags. Install: pip install -r requirements.txt
